Go to EasyRentSetup then Release Folder and install the EasyRent setup application. The User Manual is already included in the folder.

*Note: As for the update feature, kindly update only the items or users from 1-10. Update only works for those with complete data.

Server connection:
Server name = localhost
Username = root
Password = mdbanares
Database name = rental_db

Excel Reports can be found in:
UXEDP>ReportsXLS
